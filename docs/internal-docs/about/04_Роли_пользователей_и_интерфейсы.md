# Роли пользователей и интерфейсы

Система предусматривает три основных интерфейса (страницы) для различных ролей в сессии:

### Интерфейс преподавателя (админ-панель)

Преподаватель управляeт ходом интерактива через административную панель. Это защищенная веб-страница, на которой доступны следующие функции:  
- Запуск и переключение режимов: Преподаватель выбирает, какой режим активности включить (Q&A, викторина, чат и т.д.), и может переключаться между ними. Например, в начале лекции включить сбор вопросов от студентов, затем отключить его и запустить викторину. Все это делается нажатием соответствующих кнопок на панели.  
- Модерация контента: В реальном времени на панель приходят вопросы, ответы, сообщения студентов. У каждого входящего сообщения есть опции: удалить (скрыть от всех), отметить как отвеченное, при необходимости отредактировать (например, исправить опечатку или убрать нецензурную лексику перед показом на экране). Если включена предварительная модерация, преподаватель видит новые вопросы в приватном списке и решает, публиковать ли их на общий экран.  
- Управление отображением: Админ-панель позволяет выбрать, что именно показывается на проекторе. Лектор может “заморозить” отображение определенного вопроса (чтобы обсудить его, не отвлекаясь на поступление новых), или, наоборот, прокручивать список. Также можно переключаться между слайдами презентации, если она загружена, прямо из панели, совмещая выступление с интерактивом.  
- Настройки сессии на лету: В админ-интерфейсе доступны переключатели настроек: включение/выключение анонимности, чата, реакций, рейтинга и т.п. Эти опции можно менять по ходу занятия. Например, лектор может сначала держать чат выключенным, а на время обсуждения включить его одним кликом.  
- Отображение статистики: В ходе сессии преподавателю видны некоторые показатели: число подключенных участников, общее количество заданных вопросов, текущие лидеры активности (если не анонимно), процент проголосовавших в опросе и т.д. Эти данные отображаются в уголке панели и помогают ориентироваться (например, видеть, что из 100 присутствующих ответ в викторине дали уже 90 – можно завершать опрос).

Админ-панель доступна только преподавателю (требуется авторизация). Для удобства она может быть открыта на ноутбуке лектора или на планшете. Интерфейс преподавателя более насыщенный, чем у студентов, так как содержит элементы управления и модерации, однако он спроектирован интуитивно: все основные действия выполняются в один-два клика, чтобы лектор не отвлекался от ведения занятия.

### Проекторный экран (публичный дисплей)

Проекторный интерфейс – это упрощенная веб-страница, предназначенная для демонстрации контента сессии всему залу через проектор. Она не интерактивна сама по себе (ею управляет админ-панель). Основные характеристики:  
- Отображение QR-кода приглашения: при запуске новой сессии на экране крупно показывается QR-код и код сессии для ручного ввода. Это первое, что видят студенты, заходя в аудиторию. Как только достаточное число участников подключилось, преподаватель может свернуть QR-код – например, уменьшить его и сдвинуть в угол экрана, чтобы он не закрывал слайды.  
- Вывод контента интерактивов: в зависимости от активного режима, на проекторе отображаются либо список вопросов/ответов, либо текущий вопрос викторины и варианты, либо окно чата. Проекторный экран динамически обновляется по мере поступления новых данных. Например, когда студенты задают вопросы, список будет автоматически пополняться новыми строками (возможно, с анимацией). Если идет голосование или викторина, экран может показывать таймер до окончания голосования, затем – диаграмму с результатами.  
- Фокус на ключевом: Преподаватель через админ-панель может выделять определенные элементы на экране. Например, если он начинает отвечать на конкретный вопрос из списка, можно подсветить его на проекции или увеличить шрифт, чтобы все видели, на какой вопрос идет ответ. После ответа вопрос помечается как решенный (цветом или значком).  
- Реакции и анимации: Если включены эмодзи-реакции, то на проекторном экране отображаются соответствующие анимации – пролетающие смайлики, сердечки и т.п., отражающие текущие эмоции аудитории. Это делает просмотр экрана более живым и эмоциональным. Реакции отображаются ненавязчиво, чтобы не перекрывать основной контент.  
- Минимализм интерфейса: На самом проекторном интерфейсе нет элементов управления (кроме, возможно, кнопки развернуть на весь экран). Дизайн сделан с расчетом на читаемость издалека: крупный шрифт, контрастные цвета, адаптация под разные разрешения проекторов. Проекторная страница автоматически масштабируется под размер экрана, будь то небольшой экран в аудитории или большой зал.

### Интерфейс студента (веб-приложение участника)

Каждый студент, перейдя по QR-коду или ссылке, попадает на страницу участия в сессии:  
- Приветственный экран: Если сессия анонимная, студент сразу видит название лекции/события и кнопку "Присоединиться". Нажатие мгновенно подключает его к сессии. Если сессия требует авторизации, пользователь будет перенаправлен на страницу входа – либо через учетную запись университета (SSO), либо предложено ввести имя/ник. (В MVP можно реализовать упрощенно: запрос имени без пароля, чтобы не задерживать вход, но с пометкой, что нужно ввести настоящее имя.)  
- Основной экран с вкладками: После входа студент видит основной интерфейс, содержащий несколько вкладок или разделов для разных видов активности: - "Вопросы" (Q&A): здесь отображается список вопросов от студентов (в режиме обратного Q&A) или указано задание от преподавателя и поле для ответа (в режиме сбора ответов). Студент может ввести свой вопрос/ответ в текстовое поле и отправить. В списке вопросов отображаются все вопросы аудитории, под каждым – счетчик лайков. Студент может нажать значок "👍" рядом с вопросом, который ему тоже интересен, чтобы проголосовать за него. Свои отправленные вопросы он тоже видит в общем списке.  
- "Викторина": этот раздел (или всплывающее окно) активируется, когда преподаватель запускает опрос/викторину. Студенту показывается вопрос и варианты ответа (например, кнопки A, B, C, D). Он выбирает один вариант и подтверждает. После ответа интерфейс может показать отметку, что ответ принят, и ожидание результатов. Когда преподаватель раскроет правильный ответ, студент увидит сообщение о том, верно ли он ответил, и, возможно, пояснение. Если викторина включала несколько вопросов подряд, интерфейс будет последовательно их показывать.  
- "Чат": при включенном чате студент может перейти в раздел чата. Там отображаются сообщения всех участников (снизу вверх, с меткой времени). Если сессия анонимная, сообщения помечаются как "Аноним 1", "Аноним 2" или случайными аватарами; если нет – отображается имя отправителя. Внизу поля ввода студент набирает сообщение и отправляет. Новые сообщения мгновенно появляются у всех участников.  
- Реакции: элементы реакций обычно вынесены отдельно от вкладок – например, всегда на экране есть кнопка "👍" или смайлик, открывающий панель реакций. Студент может в любой момент нажать реакцию, не покидая текущую вкладку. После нажатия его интерфейс может кратко отобразить подтверждение (например, значок эмоции вспыхнет), чтобы показать, что реакция отправлена.  
- Навигация и удобство: Студенческий интерфейс разработан максимально простым. Между разделами (вкладками) можно переключаться одним нажатием. Активная вкладка подсвечена. Интерфейс адаптирован под экран смартфона: прокрутка списков (например, списка вопросов или чата) оптимизирована, крупные интерактивные элементы легко нажать пальцем. Нет перегруженности информацией: если активна викторина, то, к примеру, список вопросов временно скрыт или свернут, чтобы не мешать.  
- Обновление данных: Все представления (список вопросов, чат, результаты) обновляются в режиме реального времени без перезагрузки страницы. Благодаря использованию технологий вроде WebSocket, новые сообщения и вопросы появляются у студентов мгновенно, а лайки сразу увеличивают счетчик напротив вопроса. Это создает ощущение живого присутствия и синхронности для всех пользователей – у всех отображается одна и та же актуальная информация.

В совокупности эти три интерфейса (админ-панель, проектор, приложение студента) обеспечивают полный цикл взаимодействия. Подобная архитектура принята во многих системах для конференций и обучения: к примеру, Pigeonhole Live разделяет панель модератора/администратора, экран для аудитории и веб-приложение для участников. Наша реализация придерживается проверенных подходов, адаптированных под университетские нужды.